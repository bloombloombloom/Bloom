environments:
  default:
    shutdownPostDebugSession: true

    tool:
      name: "xplained-pro"

    target:
      name: "atmega4809"
      physicalInterface: "updi"
      hardwareBreakpoints: true

    server:
      name: "avr-gdb-rsp"
      ipAddress: "127.0.0.1"
      port: 1442
      rangeStepping: false

insight:
  activateOnStartup: false
