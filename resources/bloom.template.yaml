environments:
  default:
    debugTool:
      name: "atmel-ice"

    target:
      name: "avr8"
      physicalInterface: "debug-wire"

    debugServer:
      name: "avr-gdb-rsp"
      ipAddress: "127.0.0.1"
      port: 1442

insight:
  activateOnStartup: true
